const styles = theme => ({
  filterOption: {
    marginBottom: 0,
  },
  select: {
    marginLeft: theme.spacing(2),
    marginRight: theme.spacing(2),
  },
  text: {
    marginLeft: theme.spacing(2),
    marginRight: theme.spacing(2),
  },
  checkboxContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
export default styles;
