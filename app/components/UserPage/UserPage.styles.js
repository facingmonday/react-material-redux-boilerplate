const styles = theme => ({
  container: {
    display: 'flex',
    flex: 1,
    padding: theme.spacing(1),
  },
  formControl: {
    marginTop: theme.spacing(4),
  },
  button: {
    width: '150px',
  },
  buttonContainer: {
    flex: 1,
    alignItems: 'center',
  },
});
export default styles;
